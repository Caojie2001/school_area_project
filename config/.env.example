# 高校建筑面积缺口计算器 - 环境配置文件模板
# 复制此文件为 .env 并修改相应配置

# 服务器端口配置
# 应用程序运行的端口号，默认为3000
PORT=3000

# MySQL数据库配置
# 数据库服务器地址（本地通常为localhost）
DB_HOST=localhost

# 数据库端口（MySQL默认端口为3306）
DB_PORT=3306

# 数据库用户名
DB_USER=root

# 数据库密码（请替换为您的实际密码）
DB_PASSWORD=your_password_here

# 数据库名称
DB_NAME=school_area_management

# 文件存储配置
# Excel报告输出目录
OUTPUT_DIR=output

# 保留的历史文件数量（避免磁盘空间占用过多）
KEEP_FILES_COUNT=5

# 可选配置
# 数据库连接池配置
# DB_CONNECTION_LIMIT=10
# DB_ACQUIRE_TIMEOUT=60000
# DB_TIMEOUT=60000

# 应用程序配置
# NODE_ENV=production
# LOG_LEVEL=info
